apply from: gradlesPath + "springboot.gradle"

repositories {
    //modbus4j
    maven { url "https://maven.mangoautomation.net/repository/ias-release/" }
}

dependencies {
    compile project(":io:modbus:modbus-abstract")
}